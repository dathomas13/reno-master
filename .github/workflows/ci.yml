name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      # no package-lock.json yet: it is generated on the first local install and should
      # be committed afterwards, then this can go back to `npm ci`
      - run: npm install --no-audit --no-fund
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test

  model:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      # the generated model files must match the data base they come from
      # the committed scenes must be exactly what the house files produce
      - run: python3 tools/model/check_source.py
      - run: python3 tools/model/build_rooms.py
      - run: python3 tools/model/build_plans_svg.py
      # the frozen test data both builders are held against must be what Python makes
      - run: git diff --exit-code tools/model/testdata
