"""Rooms of the target state - starts as a copy of the existing rooms.

Change room rectangles here once walls move in haus_model_soll.py. Keep ids stable
where a room keeps its identity, so existing diary entries and costs stay linked.
"""
from rooms_ist import ROOMS  # noqa: F401
