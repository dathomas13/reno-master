// The app talks to the plugin through Capacitor.Plugins.AppUpdate (see
// src/platform/appUpdate.ts). The package exists for the native side: Capacitor finds the
// Android module through the "capacitor" field in package.json.
export {};
