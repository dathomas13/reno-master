// The app talks to the plugin through Capacitor.Plugins.Contacts (see
// src/platform/contactsImport.ts), so there is nothing to export here. The package exists
// for the native side: Capacitor finds the Android module through the "capacitor" field in
// package.json and registers the plugin automatically.
export {};
